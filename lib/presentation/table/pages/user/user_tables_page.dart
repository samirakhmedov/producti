import 'package:flutter/material.dart';

class UserTablesPage extends StatelessWidget {
  const UserTablesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
