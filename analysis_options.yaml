include: package:lint/analysis_options.yaml

analyzer:
  exclude: [
    'lib/*.config.dart',
    'lib/*.g.dart',
    'lib/generated/**/*.dart'
    ]

linter:
  rules:
    sort_pub_dependecies: false

